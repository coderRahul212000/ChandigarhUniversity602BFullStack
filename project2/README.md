#React Lifecycle Methods
1) 3 lifecycle phases 
    a) Mounting -> runs only once when the component is first rendered
    b) Updating -> runs whenever the specified state or prop changes
    c) Unmounting -> when the component is removed, React executes the cleanup function returned by the useEffect